import './index.css'

const LoginButton = (props) (
  const {text} = props
  return (
    <button type='button' onClick={this.clickButton}>
      {buttonText === 'Login' ? "Login" : 'Logout'}
    </button>
  )
)
export default LoginButton